# Auth Portal for provider

- Start the webserver via `npm start` 

## Usage

- Go to http://localhost:3001
- Authenticate by clicking on the authenticate link
- Call the service by clicking the call service button
